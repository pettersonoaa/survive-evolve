# Prey sprite sheet

- `prey_sheet.png` — **8 frames**: deer idle 0–3, hare idle 4–7
- Generate: `godot --path . --headless -s res://scripts/tools/generate_prey_sheet.gd`
- Runtime: `PreySpriteAtlas` (procedural fallback if missing)
